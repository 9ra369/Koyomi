# Koyomi 仕様書

## 1. 概要

Koyomi は、進行中の複数プロジェクトについて労働時間を記録・可視化するための
デスクトップアプリケーション。プロジェクトごとにタイマーを使って作業時間を計測し、
データはローカルに保存する。最終的に Electron でデスクトップアプリとして実装する。

[demo.html](demo.html) は Electron 化前の機能検証用プロトタイプ（単一 HTML ファイル、
ブラウザ動作、`localStorage` 保存）として引き続き残しているが、以後の主開発は
Electron 版（[main.js](main.js) ほか）を対象とする（§13）。

## 2. 目的

- 個人が関わっている複数プロジェクトの合計労働時間を把握する
- プロジェクト単位でどれくらい時間を使っているかをタイマーで記録する
- 記録したデータをローカルに保持し、外部サービスに依存しない

## 3. 用語

| 用語 | 意味 |
|---|---|
| プロジェクト | 労働時間を計測する単位。名前・色・累積時間を持つ |
| セッション | タイマーの開始〜停止の1回分の記録（開始時刻・終了時刻） |
| アクティブタイマー | 現在稼働中のタイマー。アプリ全体で最大1つ（§7.1） |
| ハートビート | アクティブタイマー稼働中に一定間隔で記録する生存確認用タイムスタンプ（§5.2） |
| 手動申請 | タイマーを使わず「この日のこの時間〜この時間に作業した」と事後に申告するセッション登録方法（§5.7） |
| 承認 | 手動申請されたセッションを集計対象として確定させる操作。承認されるまでそのセッションは合計時間に含まれない（§5.7） |

## 4. 決定事項一覧（ADR）

レビューを受けて、以下は「決定済み」として扱う。理由も添える。

| ID | 項目 | 決定 | 理由 |
|---|---|---|---|
| ADR-1 | 永続化方式 | 単一の JSON ファイル（`data.json`）。SQLite は採用しない | 想定データ量は5年分でも約1.5MBで、全件メモリ展開が十分現実的（§8.2） |
| ADR-2 | IPC設計 | レンダラーから呼ぶのは `loadState()` / `saveState(state)` の2本のみ | 状態全体が小さく、細粒度APIにする実利が薄い。preload公開面が小さくセキュリティ上も有利 |
| ADR-3 | 配布 | `electron-builder` の未署名ビルド、自動更新なし | 個人利用が前提のため |
| ADR-4 | データモデル | アクティブタイマーはトップレベルの単一フィールド `activeTimer` として持つ（`Project.running`／`currentStart` は廃止） | 「同時に1つまで」という不変条件を型で保証するため（§7.1） |
| ADR-5 | セッション構造 | `Session` はフラットな配列で持ち、`projectId` で紐づける。`id` を必須で持つ | 期間横断集計のしやすさ、将来の編集・削除機能、SQLite移行時の互換性のため（§7.2） |
| ADR-6 | スキーマバージョン | 保存データのルートに `schemaVersion` を持つ | 将来のマイグレーションを見越し、今入れておけばコストはほぼゼロ |
| ADR-7 | 日付・月境界 | 「本日」は 0時固定、「今月」はローカル暦月（1日0時〜翌月1日0時）固定。ユーザー設定は現時点で不可 | 過剰仕様を避ける。深夜作業向けのカスタム境界・会計月開始日のカスタマイズは9章で先送り |
| ADR-8 | Electronセキュリティ既定値 | `contextIsolation: true` / `nodeIntegration: false`、preload の `contextBridge.exposeInMainWorld` 経由でのみAPI公開。ファイルI/Oはメインプロセスに閉じる | Electronの標準的な安全構成 |
| ADR-9 | データ保存場所 | `app.getPath('userData')` ではなく、アプリ直下の `data/` フォルダに保存する | ユーザーの明示的な指定。個人利用・単一マシン運用のため、配布可搬性よりも「どこにデータがあるか一目でわかること」を優先した。パッケージ配布時にインストール先が書き込み不可な場所になる場合は再検討する |
| ADR-10 | 手動申請の扱い | 手動入力したセッションは `status: "pending"` として保存し、承認するまで集計に含めない。タイマー計測によるセッションは自動的に `status: "approved"` とする | 単独利用でも「実測」と「事後の自己申告」を区別できるようにし、集計の信頼性を保つため（§5.7） |
| ADR-11 | 集計スコープ | 画面上の主要な合計表示（全体サマリー・プロジェクトごとの累積時間）は「全期間」ではなく「閲覧中の月」を対象にする。月が変わると自動的に0から再開する。過去の月は◀▶ナビゲーションで閲覧できる読み取り専用ビューとし、`sessions` 自体は削除・移動しない | ユーザーの明示的な指定（月ごとの労働時間管理）。`sessions` はタイムスタンプを持つフラット配列のため（ADR-5）、月次集計は既存データに対するフィルタで実現でき、月替わりのたびにデータを移動・アーカイブする必要がない |
| ADR-12 | エクスポート形式 | JSONのみ。CSVエクスポートは提供しない。JSON出力の日時は epoch ms のままではなく `YYYY_MMDD_HHmm`（24時間表記）の文字列に変換する | ユーザーの明示的な指定。エクスポートは人が読んで確認する用途が主であり、内部保存形式（epoch ms）をそのまま出すより可読性を優先した |

これにより、旧版8章の「要検討: JSONで十分かSQLiteが必要か」「パッケージング（要検討）」、
9章の「IPC設計」は解消済みとする。

## 5. 機能要件

要件には `FR-<節番号>-<連番>` の形式でIDを振る。実装・テストと対応づけて参照する。

### 5.1 プロジェクト管理

| ID | 要件 |
|---|---|
| FR-5.1-1 | プロジェクトを新規追加できる（名前を入力して追加。空文字・空白のみは無視） |
| FR-5.1-2 | プロジェクト名は後から編集できる（インライン編集） |
| FR-5.1-3 | プロジェクトは複数登録・一覧表示できる |
| FR-5.1-4 | 各プロジェクトに識別用の色を自動割り当てする。パレットは固定N色を順に循環し、重複を許容する。削除後に再追加したプロジェクトの色は先着順に再割り当てされ、以前の色との一致は保証しない |
| FR-5.1-5 | 色に加えて名前を必ず併記する（色のみでの識別に依存しない。色覚特性への配慮） |
| FR-5.1-6 | プロジェクトは削除ではなく **アーカイブ** できる。アーカイブされたプロジェクトは一覧から隠れるが、セッション記録は保持され、集計（全期間合計など）にも含まれ続ける |
| FR-5.1-7 | 完全削除は「アーカイブ済みのプロジェクト」に対してのみ許可する。実行前に確認ダイアログを出す |

> **旧仕様からの変更点**: 「削除に確認ダイアログ」のみだった振る舞いを、アーカイブ優先に変更（§1.4 のレビュー指摘に対応）。確認ダイアログは事故防止として弱いため、まず不可逆でない手段を用意する。

### 5.2 タイマー

| ID | 要件 |
|---|---|
| FR-5.2-1 | プロジェクトごとに開始／停止ボタンでタイマーを操作できる |
| FR-5.2-2 | 稼働中のプロジェクトはリアルタイムで経過時間が更新表示される（1秒間隔）。この更新表示は再計算のみで、保存I/Oを伴わない |
| FR-5.2-3 | アクティブタイマーはアプリ全体で最大1つ（ADR-4）。あるプロジェクトのタイマーを開始したとき、既に別プロジェクトが稼働中であれば、そのプロジェクトのタイマーを自動停止してセッションを確定する |
| FR-5.2-4 | 自動停止が発生したことをトースト通知でユーザーに知らせる（例:「〇〇を停止しました（1:23:45）」）。無言での自動停止は行わない |
| FR-5.2-5 | 稼働中は30〜60秒間隔で `activeTimer.lastHeartbeat` を更新・保存する（ハートビート） |
| FR-5.2-6 | アプリ起動時、`activeTimer` が存在し `now - lastHeartbeat` が閾値（既定5分）を超えている場合、ユーザーに次の3択を提示する: (a) `lastHeartbeat` の時刻で停止として確定する（推奨・既定選択） (b) そのまま現在時刻まで継続する (c) このセッションを破棄する |
| FR-5.2-7 | 1セッションが上限時間（既定12時間）を超えて継続している場合、自動的に停止しセッションを確定した上で通知する |
| FR-5.2-8 | セッション長が最小しきい値（既定10秒）未満で停止された場合、セッションとして確定・保存しない（誤操作対策） |
| FR-5.2-9 | システム時計の巻き戻り等により `end < start` となった場合、そのセッションの長さは 0 として扱う（負の値を許容しない） |

**受け入れ基準（FR-5.2-3 に対する不変条件）**:
> 稼働中に別プロジェクトのタイマーを開始したとき、自動停止されるプロジェクトのセッションの
> `end` は、新しく開始されるセッションの `start` と**同一の値**になる。時間の欠落・重複を
> 生じさせない。

### 5.3 履歴

| ID | 要件 |
|---|---|
| FR-5.3-1 | プロジェクトごとに過去のセッション一覧を開閉可能な形で表示できる |
| FR-5.3-2 | 各セッションは開始日時・終了日時・所要時間を表示する |
| FR-5.3-3 | セッションは日付ごとにグルーピングし、直近7日分を初期表示、それ以前は「もっと見る」で展開する |
| FR-5.3-4 | セッションは終了時刻の編集、および削除ができる（停止し忘れ等の事後修正のため） |

> **旧仕様からの変更点**: 「20件程度」という根拠のない件数制限を廃止し、日付グルーピング
> ＋段階的開示に変更。手動編集・削除は「停止し忘れ」対策（FR-5.2-6/7/8）だけでは
> 拾いきれない事後修正を担保するため必須とする。

### 5.4 集計・サマリー

| ID | 要件 |
|---|---|
| FR-5.4-1 | 閲覧中の月の合計労働時間を表示する（§5.8。旧仕様の「全プロジェクト合計」から変更、ADR-11） |
| FR-5.4-2 | 本日分の合計労働時間を表示する。**稼働中セッションの経過時間もこの合計に含める**（動いているのに数字が増えないという直感に反する表示を避けるため） |
| FR-5.4-3 | 現在稼働中のプロジェクト数を表示する |
| FR-5.4-4 | プロジェクトごとに「本日の労働時間」「セッション件数」を一覧上に表示する。「本日」の切り出しロジックは FR-5.4-2 と同一の共通関数を使う（実装を分岐させない）。「セッション件数」は閲覧中の月に含まれる承認済みセッション数とする（§5.8） |
| FR-5.4-5 | 「本日」はローカルタイムの 0 時を境界とする（ADR-7）。過去セッションは記録時の絶対時刻（epoch ms）のまま保持し、表示のたびに現在のローカルタイムゾーンで解釈し直す |
| FR-5.4-6 | 日別・週別の集計・グラフ表示は本バージョンのスコープ外とする（§9）。月次の合計値は §5.8 で対応済み。日別・週別のグラフ化が必要になった場合も、§7.2 のデータ構造（フラットな `sessions[]`）から集計ロジックのみで実現できることを前提に設計する |
| FR-5.4-7 | すべての集計（月の合計・本日の合計・プロジェクト別合計）は `status: "approved"` のセッションのみを対象とする（ADR-10）。`pending`／`rejected` のセッションは金額・時間として計上しない |

### 5.5 データ保存・エクスポート

| ID | 要件 |
|---|---|
| FR-5.5-1 | 全データ（プロジェクト、セッション履歴、アクティブタイマー状態）をローカルに保存する。デモ版は `localStorage`、Electron版は単一 JSON ファイル（ADR-1、§8） |
| FR-5.5-2 | 保存は以下のイベントでのみ発生する: プロジェクト追加/改名/アーカイブ/削除、タイマー開始/停止、セッション編集/削除、ハートビート更新。1秒ごとの表示更新（FR-5.2-2）は保存を伴わない |
| FR-5.5-3 | 短時間に連続する変更は保存をデバウンスする（既定500ms） |
| FR-5.5-4 | 全データをJSON形式でエクスポートできる。可読性のため、`Session.start`/`end`・`Project.createdAt`・`ActiveTimer.startedAt`/`lastHeartbeat` は `YYYY_MMDD_HHmm` 形式（24時間表記、例: `2026_0811_2320`）の文字列に変換してから出力する。内部の保存形式（epoch ms、ADR-1）とは異なる、エクスポート専用の表現であり、逆方向のインポート機能は現時点で提供しない |
| FR-5.5-5 | CSVエクスポートは提供しない（ADR-12）。エクスポートはJSON形式のみとする |

### 5.6 エラー表示

| ID | 要件 |
|---|---|
| FR-5.6-1 | 保存に失敗した場合、ユーザーに通知する |
| FR-5.6-2 | 起動時にデータファイルのパースに失敗した場合、直前世代のバックアップ（§8.3 の `.bak`）へ自動フォールバックし、その旨をユーザーに通知する |
| FR-5.6-3 | バックアップへのフォールバックにも失敗した場合、空の状態で起動しつつ、破損したファイルを別名で退避し、ユーザーに明示する（データを黙って失わない） |

### 5.7 手動申請・承認

タイマーを回し忘れた場合や、後から作業時間を思い出して記録したい場合のための、
事後入力の手段。承認という関門を挟むことで、実測（タイマー）と自己申告（手動入力）を
区別し、集計の信頼性を保つ。

| ID | 要件 |
|---|---|
| FR-5.7-1 | プロジェクトごとに「時間を申請」操作から、開始日時・終了日時・メモ（任意）を入力してセッションを登録できる |
| FR-5.7-2 | 手動申請で作成されたセッションは `source: "manual"`、`status: "pending"` として保存される。タイマーで確定したセッションは `source: "timer"`、`status: "approved"` として保存される |
| FR-5.7-3 | `pending` のセッションは集計に含まれない（FR-5.4-7）。履歴上では「承認待ち」であることが分かる表示にする |
| FR-5.7-4 | `pending` のセッションは「承認」または「却下」できる。承認すると `status: "approved"` になり以降の集計に含まれる。却下すると `status: "rejected"` になり、履歴には残るが集計には含まれない |
| FR-5.7-5 | 申請時の入力値は最低限の妥当性検証を行う: 終了日時は開始日時より後であること、1件あたりの長さは24時間以内であること。違反時はトーストで理由を表示し、登録しない |
| FR-5.7-6 | 承認者は現時点ではアプリ利用者本人のみで、承認操作に認証は要求しない（個人利用が前提のため。NFR-3）。将来的に複数人での利用に広げる場合の拡張点として、承認という操作自体は最初から用意しておく |
| FR-5.7-7 | 開始・終了日時は、日付と時刻を分けた入力とする。日付はネイティブの `<input type="date">`（カレンダーで選択できる）、時刻はテキスト入力（`HH:MM`）とする。`datetime-local` ピッカー（日付・時刻が1つのUIに混在し操作が煩雑）は採用しない。形式が不正な場合はトーストで理由を示し、登録しない |
| FR-5.7-9 | 時刻入力は `HH:MM` 以外に、コロン省略の3〜4桁数字（例: `1145` → `11:45`、`945` → `09:45`）も受け付ける。入力欄からフォーカスが外れた時点で `HH:MM` 表記に整形して表示し、解釈結果を確認できるようにする |
| FR-5.7-10 | 「時間を申請」モーダル、およびセッションの編集フォームは、フォーム内のどの入力欄にフォーカスがあっても Ctrl+Enter（macOSでは Cmd+Enter）で確定（申請／保存）できる |
| FR-5.7-8 | 申請・編集する時間帯が、却下済み以外の既存セッション（他プロジェクトを含む）または稼働中のタイマーと重複する場合は登録・保存せず、トーストで理由を示す。1人が同時刻に複数の作業をしたことにはならないという前提（ADR-4 の単一アクティブタイマーと同じ考え方）による |

> **設計メモ**: 承認は「別の人が確認する」ためではなく、「タイマーで測っていない時間」を
> 明示的に区別するための仕組みとして導入する。将来的に運用が変わっても、
> `source`/`status` の2フィールドだけで表現できるようにしてある（§7.1）。

### 5.8 月次管理

月ごとに労働時間を管理する。月が変わると主要な合計表示は自動的に0から再開するが、
過去のセッションデータは削除・移動されず、月を遡って閲覧できる（ADR-11）。

| ID | 要件 |
|---|---|
| FR-5.8-1 | 画面には「閲覧中の月」の状態を持ち、既定値は現在の月とする。全プロジェクト合計・プロジェクトごとの累積時間（従来「全期間」だった値）は、閲覧中の月に属する承認済みセッションのみを対象に算出する |
| FR-5.8-2 | ◀／▶ の操作で閲覧中の月を前後に切り替えられる。月ラベル（例:「2026年8月」）を表示する |
| FR-5.8-3 | 未来の月には進めない（▶は閲覧中の月が現在の月のとき無効化する） |
| FR-5.8-4 | 現在の月を閲覧しているときのみ、各プロジェクトの開始／停止操作を表示する。過去の月を閲覧しているときはタイマーを開始できない（過去の日時に対してタイマーを操作する意味がないため。時間を追加したい場合は §5.7 の手動申請を使う） |
| FR-5.8-5 | 月の境界はローカルタイムの暦月（1日0時〜翌月1日0時）で固定する（ADR-7）。会計月開始日のカスタマイズは§11で先送り |
| FR-5.8-6 | 月が変わっても `sessions` は削除・移動されない。月ごとの合計は保存されている絶対時刻から都度算出する（集計値そのものは保存しない。ADR-11） |
| FR-5.8-7 | 個々のセッションの履歴一覧（FR-5.3）は月の切り替えによる影響を受けず、常に全期間を対象に日付グルーピング表示する。月次ナビゲーションは「合計値」のスコープを切り替えるものであり、履歴の可視性を制限するものではない |

**受け入れ基準（FR-5.8-1 に対する不変条件）**:
> 9月になった直後にアプリを開くと、閲覧中の月は自動的に9月になり、全プロジェクト合計・
> 各プロジェクトの累積時間は 00:00:00 から始まる。◀ を1回押すと8月に切り替わり、
> 8月分の実績がそのまま（1秒たりとも欠けずに）表示される。

## 6. 非機能要件

| ID | 要件 |
|---|---|
| NFR-1 | オフラインで完結して動作すること（外部通信なし） |
| NFR-2 | タイマー表示は毎回 `Date.now()` との差分で再計算するため、表示のずれは累積しない |
| NFR-3 | 個人利用が前提のため、認証・マルチユーザー対応は不要 |
| NFR-4 | クラウド同期は本バージョンのスコープ外（JSONエクスポートによる手動移行で代替する） |
| NFR-5 | 通常想定するデータ量（5年分・約12,500セッション・約1.5MB）において、起動時の読み込みおよび保存は体感で遅延を感じさせない（目安: 200ms未満） |

## 7. データモデル

### 7.1 全体構造

アクティブタイマーはプロジェクトに内包せず、トップレベルの単一フィールドとして持つ（ADR-4）。
これにより「同時に1つまで」という不変条件が構造上保証される。将来複数同時稼働を許可する場合も、
`activeTimer: ActiveTimer | null` を `activeTimers: ActiveTimer[]` に変えるだけで済み、
`Project` 側の変更は不要。

```ts
type AppState = {
  schemaVersion: 1;
  projects: Project[];
  sessions: Session[];
  activeTimer: ActiveTimer | null;
};

type ActiveTimer = {
  projectId: string;
  startedAt: number;      // epoch ms
  lastHeartbeat: number;  // epoch ms（FR-5.2-5）
};

type Project = {
  id: string;
  name: string;
  color: string;        // 識別用カラーコード（FR-5.1-4）
  createdAt: number;     // epoch ms
  archived: boolean;     // FR-5.1-6
};

type Session = {
  id: string;
  projectId: string;
  start: number;   // epoch ms
  end: number;      // epoch ms
  note?: string;    // 任意の一行メモ（何をしていたか）
  source: "timer" | "manual";       // FR-5.7-2
  status: "approved" | "pending" | "rejected"; // FR-5.7-2〜4
};
```

`source`/`status` を持たない古いデータ（本機能追加前に保存されたセッション）を読み込んだ場合は、
`source: "timer"`, `status: "approved"` として扱う（後方互換のための実行時デフォルト。
`schemaVersion` 自体は変更しない。ADR-6 参照）。

### 7.2 設計メモ

- `Project.running` / `currentStart` は廃止し `activeTimer` に一本化した（ADR-4）。
- `Session` は `Project.sessions[]` への内包をやめ、フラット配列 + `projectId` に変更した（ADR-5）。
  理由: 期間横断の集計（§9で先送りした日別/週別集計）が基本操作になった場合に、
  ネストを毎回平坦化する必要がなくなる。また1セッション追加のたびにプロジェクト
  オブジェクト全体を書き換える必要がなくなり、保存の粒度が細かくなる。
- 累積時間は保存せず、`sessions` から都度導出する。稼働中であれば `activeTimer` の
  経過時間を加算する（FR-5.4-2）。
- `note` は将来の手動編集機能（FR-5.3-4）と合わせて活用できる。コストが小さく効用が大きいため
  先に型へ入れておく。
- `schemaVersion` は読み込み時に必ず参照し、将来のスキーマ変更時はマイグレーション関数を通す
  （ADR-6）。

## 8. 永続化・保存戦略

### 8.1 保存タイミング

FR-5.5-2 / FR-5.5-3 の通り、保存はイベント駆動＋デバウンスとする。1秒ごとの表示更新
（FR-5.2-2）はメモリ上の再計算のみで、ディスクI/Oを発生させない。

### 8.2 保存方式（ADR-1）

概算根拠: 1日10セッション × 250営業日 × 5年 ≒ 12,500件。1件約120バイトとして
約1.5MB。全件をメモリに載せて毎回再計算しても体感上の問題はない規模のため、
単一の JSON ファイルで十分と判断する。SQLite等の導入は次の条件に達した時点で
初めて再検討する（それまでは検討自体を行わない）:

- 起動時のパースが体感で遅いと分かったとき
- セッション数が数万件を超えたとき

### 8.3 ファイル構成とアトミック書き込み

保存先はアプリ直下の `data/` フォルダ（ADR-9）、単一ファイルにまとめる（プロジェクトと
セッションを別ファイルに分割すると、片方だけ書き込まれた中間状態が生じうるため）。

```
data.json      現行データ（AppState を丸ごと1ファイル）
data.json.bak  直前世代のバックアップ
data.json.tmp  書き込み中の一時ファイル
```

生JSONファイルは書き込み中のクラッシュで全損しうるため、単純な `writeFile` は行わず
アトミックな置き換えを行う:

```js
await fs.writeFile(tmpPath, json, 'utf8');
await fs.rename(currentPath, bakPath).catch(() => {}); // 初回はファイルが存在しない
await fs.rename(tmpPath, currentPath);                  // ここがアトミック
```

起動時にパースエラーが発生した場合は `.bak` へフォールバックする（FR-5.6-2）。

## 9. 画面仕様

### 9.1 ヘッダー

- アプリ名・サブタイトルを表示

### 9.2 プロジェクト追加フォーム

- テキスト入力＋「追加」ボタン
- Enter キーでも追加可能
- 空文字・空白のみの入力は無視する（FR-5.1-1）

### 9.3 サマリーバー

- 月ナビゲーション（◀／月ラベル／▶）＋「月の合計」「本日の合計」「稼働中プロジェクト」の
  3カード（FR-5.4-1〜3、FR-5.8-1/2/3）

### 9.4 プロジェクト一覧

各プロジェクトカードに以下を表示：

- 色インジケーターとプロジェクト名（クリックで編集）
- メタ情報（本日の時間・閲覧中の月のセッション件数）
- 閲覧中の月の累積時間（タイマー、稼働中は強調表示。FR-5.8-1）
- 開始／停止ボタン（現在の月を閲覧しているときのみ表示。FR-5.8-4）
- アーカイブ操作（誤クリックを避けるため、開始／停止ボタンとは分離したメニュー内に配置する。FR-5.1-6/7）
- 「時間を申請」操作（同じくメニュー内。FR-5.7-1）
- 履歴の開閉トグルとセッション一覧（日付グルーピング、FR-5.3-3）。`pending`/`rejected` の
  セッションには状態が分かるラベルを付け、`pending` には承認／却下の操作を表示する（FR-5.7-3/4）

### 9.5 通知・トースト

- 自動停止時（FR-5.2-4）、セッション上限到達時（FR-5.2-7）、保存失敗時（FR-5.6-1）、
  バックアップ復旧時（FR-5.6-2）にトーストを表示する

### 9.6 起動時の復帰確認ダイアログ

- FR-5.2-6 に対応。`activeTimer` の異常な放置を検知した場合のみ表示する3択ダイアログ

### 9.7 空状態

- プロジェクトが1件もない場合は案内メッセージを表示する

## 10. 技術スタック / Electron設計

### 10.1 現行デモ

- HTML / CSS / Vanilla JavaScript（単一ファイル、ビルド不要）
- `localStorage` によるデータ永続化
- 集計・時間計算ロジックは、次のイテレーションで DOM 操作から分離し、
  `totalDuration()` / `todayDuration()` / `splitSessionByDay()` などの
  **純粋関数モジュール**として切り出す。これは Electron 版にそのまま移植でき、
  かつ日跨ぎ・稼働中加算・時計巻き戻り（FR-5.2-9, FR-5.4-2, FR-5.4-5）といった
  不変条件を単体テストで担保できるようにするため。

### 10.2 Electron版

- メインプロセス／レンダラープロセス構成
- セキュリティ既定値（ADR-8）: `contextIsolation: true` / `nodeIntegration: false`、
  preload の `contextBridge.exposeInMainWorld` 経由でのみ API を公開する
- IPC は `loadState()` / `saveState(state)` の2本のみ（ADR-2）。ファイルI/O
  （アトミック書き込み・`.bak` フォールバック、§8.3）はすべてメインプロセス側に閉じ込める
- 稼働中はシステムトレイに稼働中プロジェクト名と経過時間を表示する
  （ウィンドウを閉じていても稼働状況が分かるようにするため）
- 開始／停止をグローバルショートカットに割り当てられるようにする（`globalShortcut`）
- パッケージング: `electron-builder` の未署名ビルド、自動更新なし（ADR-3）
- 自動起動・システムトレイ常駐を前提とした自動起動設定は行わない

## 11. 今後の検討事項

「今決める／実装しながら決める／意図的に先送り」を分類して記載する。

| 検討事項 | 分類 | 備考 |
|---|---|---|
| 複数タイマーの同時稼働 | 意図的に先送り | `activeTimer` 単体構造のままで良い。必要になれば `activeTimers[]` に変更（§7.1） |
| 日別・週別・月別の集計・グラフ | 意図的に先送り | データ構造（フラット `sessions[]`）は既に対応済み。UIは後から追加可能（FR-5.4-6） |
| プロジェクトの並び替え | 実装しながら決める | 優先度低。使ってみて必要性を判断する |
| 深夜作業向けの日付境界カスタマイズ | 意図的に先送り | ADR-7 により当面 0時固定 |
| 複数端末間でのデータ同期 | 意図的に先送り | JSONエクスポート（FR-5.5-4）による手動移行で当面代替する |

## 12. 着手順（ロードマップ）

1. データモデルの変更: `activeTimer` への一本化、`schemaVersion` 追加、`Session` の
   フラット化（§7、ADR-4〜6）
2. ハートビートと起動時の復帰確認（FR-5.2-5/6）
3. 「本日」の定義確定と集計ロジックの純粋関数化・単体テスト化（FR-5.4-2/4/5、§10.1）
4. 最小セッション長・上限時間・自動停止時のトースト（FR-5.2-7/8/9、FR-5.2-4）
5. アーカイブ機能（FR-5.1-6/7）
6. エクスポート（FR-5.5-4/5）
7. Electron移行: アトミック書き込み（§8.3）、preload/IPC（ADR-2/8）
8. 手動申請・承認（FR-5.7、ADR-10）
9. 月次管理（FR-5.8、ADR-11）
10. システムトレイ、グローバルショートカット（§10.2、未着手）

## 13. 現在の実装状況

- [demo.html](demo.html): §4〜§8 の決定事項を反映したブラウザ動作の単体プロトタイプ
  （`localStorage` 保存）。
- Electron 版（[main.js](main.js) / [preload.js](preload.js) / [renderer/](renderer/)）:
  §1〜§10 の主要機能（プロジェクト管理・タイマー・履歴・アーカイブ・エクスポート・
  ハートビートと復帰確認・手動申請と承認・月次管理）を実装済み。データ保存先は ADR-9 の通り
  アプリ直下の `data/` フォルダ。[start.bat](start.bat) から起動できる
  （初回は `npm install` を自動実行してから `npm start` を行う）
- アプリアイコン（[build/icon.svg](build/icon.svg) 源流、`build/icon.ico`・`build/icon.png`・
  `build/icons/*.png` へラスタライズ済み）を用意し、`main.js` の `BrowserWindow` に設定済み
- 未着手: システムトレイ表示、グローバルショートカット、`electron-builder` によるパッケージング
  （パッケージング時は用意済みのアイコンをそのまま参照できる）
